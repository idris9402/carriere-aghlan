# Android app shell
This folder is prepared as a Capacitor Android wrapper around the same CARRIÈRE AGHLAN Web/PWA interface.

After installing Node dependencies:
1. npm install
2. npx cap add android
3. npx cap sync
4. npx cap open android

The same frontend/API is shared with the web version.
